version: '3.8'

services:
  agent:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: officeqa-purple-agent
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - AGENT_LOG_LEVEL=INFO
    volumes:
      - ./output:/app/output
      - ./data:/app/data
    ports:
      - "8000:8000"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 5s
